import { answerLetter } from '../../../helpers/answer-letter';
import { module, test } from 'qunit';

module('Unit | Helper | answer letter');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = answerLetter(42);
  assert.ok(result);
});
