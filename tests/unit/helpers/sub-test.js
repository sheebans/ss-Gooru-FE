import { sub } from 'gooru-web/helpers/sub';
import { module, test } from 'qunit';

module('Unit | Helper | sub');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = sub([4, 1]);
  assert.equal(result, 3, 'Wrong response');
});
