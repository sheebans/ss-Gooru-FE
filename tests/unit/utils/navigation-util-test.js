import navigationUtil from 'gooru-web/utils/navigation-util';
import { module, test } from 'qunit';

module('Unit | Utility | navigation util');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = navigationUtil();
  assert.ok(result);
});
