import DropdownItem from '../../../utils/dropdown-item';
import { module, test } from 'qunit';

module('Unit | Utility | dropdown item');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = DropdownItem.create();
  assert.ok(result);
});
