
{{player.gru-study-header classId=classId collection=collection courseId=course.id courseTitle=course.title showConfirmation=showConfirmation
resourceSequence=resource.sequence actualResource=resource isPreTest=(and hasPreTestSuggestions showSuggestion) courseVersion=courseVersion
showBackToCourseMap=showBackToCourseMap showBackToCollection=showBackToCollection steps=steps }}



<div class="content"> {{outlet}}
</div>
