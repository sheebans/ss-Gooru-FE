<div class="gooru">
  {{app-header
    onLogout="logout"
    onSearch="searchTerm"}}
    <div class="container-fluid app-container ma-top-25">
      {{outlet}}
    </div>
  {{app-footer}}
</div>

{{gru-modal}}