<div class="gooru">
  {{gru-header
    onLogout="logout"
    onSearch="searchTerm"}}
    <div class="container-fluid app-container ma-top-25">
      {{outlet}}
    </div>
  {{gru-footer}}
</div>

{{gru-modal}}