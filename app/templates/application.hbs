{{{themeStyleTag}}}
<div id="{{if theme themeId 'gooru-theme'}}" class="gooru">
  {{gru-header
    onLogout="logout"
    onSearch="searchTerm"}}
    <div class="container-fluid app-container">
      {{outlet}}
    </div>
  {{gru-footer}}
</div>

{{gru-modal}}