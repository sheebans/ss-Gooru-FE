<div class="controller content-resources">
    <div class="resources">
      {{#each resources as |resource|}}
          <div class="card">
            {{cards.gru-resource-card resource=resource editEnabled=true onEditResource="editResource"}}
          </div>
      {{/each}}
    </div>
</div>
