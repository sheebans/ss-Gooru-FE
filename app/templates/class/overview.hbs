<div class="controller overview">
  <div class="fixed">
    <div class="overview-header">
      <div>
        <h5>{{t "class.overview.title"}}</h5>
      </div>
      {{#if classController.isStudent}}
        <button {{action "updateLocation" userLocation}} class="btn btn-default locate">
          {{t "class.overview.locate"}}
        </button>
      {{else}}
        <button {{action "editContent"}} class="btn btn-default edit-content">
          {{t "class.overview.editContent"}}
        </button>
      {{/if}}
    </div>
  </div>

  {{class.overview.gru-accordion-course
    currentClass=class
  onSelectResource="playResource"
  onLocationUpdate=(action 'updateLocation')
  userLocation=userLocation
  location=location }}

</div>