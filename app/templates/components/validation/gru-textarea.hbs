<div class="form-group">
  {{textarea value=rawTextareaValue class="form-control" name=valuePath key-press="textareaTyping" focus-out="textareaValueChange" rows=rows maxlength=maxLength}}
    <div class="error-messages">
      {{#if showMessage}}
          <span class="error">
            {{v-get model valuePath 'message'}}

          </span>
      {{/if}}
      {{#if isMaxLength}}
        <span class="warning">
          {{t "common.errors.collection-learning-objectives-max-length"}}
        </span>
      {{/if}}
    </div>
</div>
