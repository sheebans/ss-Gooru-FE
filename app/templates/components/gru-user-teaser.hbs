<span>
    <img class="avatar" src="{{firstUser.profileImageUrl}}">
</span>
<span class="user-full-name">
  <a {{action 'selectUser'}}>{{firstUser.fullName}}</a>
</span>
<span class="total-users-left">
  {{#if usersLeft}}
      <p>+{{usersLeft}}</p>
  {{/if}}
</span>
