<div class="resource-results">
    <div class="results">
      {{#if resourceResults}}
        {{#each resourceResults as |resource|}}
          {{new-cards.gru-resource-card resource=resource}}
        {{/each}}
      {{else}}
        {{partial 'partials/no-results-found'}}
      {{/if}}
    </div>
</div>
