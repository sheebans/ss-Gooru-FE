<strong>{{title}}</strong>
<div class="content">
    <span>{{body}}</span>
    <div class="cta">
        <input placeholder="{{text-placeholder}}" />
        <button class="btn btn-primary">{{cta-text}}</button>
    </div>
</div>

