<ul>
  {{#each answers as |answer index|}}
      <li>
          <span class="{{if answer.correct 'correct' 'incorrect'}} {{if anonymous 'anonymous'}}">{{answer.selectedOrder}}</span>
          <span class="answer-text">{{{gru-math-text text=answer.text}}}</span>
      </li>
  {{/each}}
</ul>
