<div class="title">
    <h2>{{t "common.questions"}}</h2>
</div>
<div class="switch-section">
  {{gru-switch switchOptions=switchOptions}}
</div>
<table>
    <thead>
    <tr>
        <th class="header number">
          {{t "gru-questions.no"}}
        </th>
        <th class="header question">
          {{t "gru-questions.question"}}
        </th>
        <th class="header answer">
          {{t "gru-questions.answer"}}
        </th>
        <th class="header score">
          {{t "gru-questions.score"}}
            <span>{{fa-icon icon='sort-desc'}}</span>
        </th>
        <th class="header time-spent">
          {{t "gru-questions.time-spent"}}
            <span>{{fa-icon icon='sort-desc'}}</span>
        </th>
        <th class="header reaction">
          {{t "gru-questions.reaction"}}
            <span>{{fa-icon icon='sort-desc'}}</span>
        </th>
    </tr>
    </thead>
    <tbody>
    {{#each questions as |question|}}
      <tr>
          <td class="number-question">
              {{question.order}}
          </td>
          <td class="question-text">
            {{question.question.text}}
          </td>
          <td class="question-answer">

          </td>
          <td class="question-score">
            {{#if question.correct}}
                <span>{{fa-icon icon='check-circle-o'}}</span>
            {{else}}
                <span>{{fa-icon icon='times-circle-o'}}</span>
            {{/if}}
          </td>
          <td class="question-time">
            {{question.timeSpent}}
          </td>
          <td class="question-reaction {{question.reaction}}">
              <div>
              </div>
          </td>
      </tr>
    {{/each}}
    </tbody>
</table>

