<div class="collection-results">
    <div class="row">
        <div class="text-center">
            <div class="results">
              {{#each collectionResults.collections as |collection|}}
                {{cards.gru-collection-card collection=collection}}
              {{/each}}
            </div>
        </div>
    </div>
</div>