{{#if model.isReadonly}}
  <div>
    <b>{{model.taxonomyItem.label}}</b>
    <span>{{model.taxonomyItem.caption}}</span>
  </div>
{{else}}
  <button class="toggle" {{action 'toggleState'}}>
    <div>
      <b>{{model.taxonomyItem.label}}</b>
      <span>{{model.taxonomyItem.caption}}</span>
    </div>
  </button>
{{/if}}

{{#if model.isRemovable}}
  <button class="remove" {{action 'removeTag'}}>{{gru-icon name="clear"}}</button>
{{/if}}

