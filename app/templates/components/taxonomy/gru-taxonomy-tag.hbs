{{#if model.isReadonly}}
  <span>{{model.taxonomyItem.label}}</span>
{{else}}
  <button class="toggle" {{action 'toggleState'}}>{{model.taxonomyItem.label}}</button>
{{/if}}

{{#if model.isRemovable}}
  <button class="remove" {{action 'removeTag'}}>{{gru-icon name="clear"}}</button>
{{/if}}

