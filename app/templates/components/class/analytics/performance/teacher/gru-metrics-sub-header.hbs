{{#each visibleMetrics as |metric index|}}
  {{#if metric.visible}}
    <div class="metrics-sub-header table-cell {{if (eq metric.value 'time-spent') 'pull-right' ''}} {{if (and (or showReport showStudyTime) (eq metric.value 'score')) 'pull-left' ''}}">
      <a class="metric-sub-header {{metric.value}}" {{action 'sort' metric}}>
        {{t (concat 'gru-metrics-sub-header.' metric.value) }}
      </a>
      {{#if metric.sorted}}
        {{#if metric.isAsc}}
          <span>{{gru-icon name='arrow_upward'}}</span>
        {{else}}
          <span>{{gru-icon name='arrow_downward'}}</span>
        {{/if}}
      {{else}}
        <span>{{gru-icon name='swap_vert'}}</span>
      {{/if}}
    </div>
  {{/if}}
{{/each}}
