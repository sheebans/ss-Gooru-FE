<header>
    <div class="header-content">
        <button type="button" class="btn-back" {{action "goBack"}}>
          {{gru-icon name="keyboard_arrow_left"}}  {{t 'common.back'}}
        </button>
        <div class="heading">
            <div class="information">
                <h1 title="{{{question.title}}}">{{question.title}}</h1>
                <div class="details">
                    <div class="type">
                        <i class="{{if (eq question.type 'T/F') 'T_F-icon' (concat question.type '-icon')}}"></i>
                        <span>{{t (concat "common.question-type." question.type)}}</span>
                    </div>
                  {{!--Mock Data, these should be removed with the correct component --}}
                    <div class="tags">
                        <span class="label"><span>3.OA.A.2</span> CCSS MATH</span>
                        <span class="label"><span>3.OA.A.2</span> CCSS MATH</span>
                        <span class="label"><span>3.OA.A.2</span> CCSS MATH</span>
                        <span class="extra">+3</span>
                    </div>
                </div>
            </div>
            <div class="publisher">
                <span>{{t 'common.published-by'}}</span>
                    <img src="{{ question.owner.avatarUrl }}" alt="Publisher">
                  {{#link-to 'profile.content.courses' question.owner.id class="pointer" tagName="span"}}
                    {{question.owner.displayName}}
                  {{/link-to}}
            </div>
        </div>
    </div>
</header>

<div id="content" class="player">
  {{player.gru-question-viewer
  submitted=false
  question=playerQuestion
  questionResult=questionResult
  onSubmitQuestion="submitQuestion"
  }}
</div>