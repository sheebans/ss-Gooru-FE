<div class="modal-header">
  <h4 class="modal-title">{{t "common.add-century-skills"}}</h4>
</div>
<div class="modal-body">
  {{content.collections.gru-collection-skills-legend }}
  <form>

    <div class="actions">
      <button type="button" class="btn btn-default cancel" data-dismiss="modal" aria-label="{{t 'common.close'}}">
        <span aria-hidden="true">{{t 'common.cancel'}}</span>
      </button>
      <button type="submit" class="btn btn-primary">{{t 'common.add'}}</button>
    </div>

  </form>
</div>
