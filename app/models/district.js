import Ember from 'ember';

/**
 * District model
 *
 * @typedef {Object} District
 */
export default Ember.Object.extend({
  /**
   * @property {string}
   */
  id: null,

  /**
   * @property {string}
   */
  name: null,

  /**
   * @property {string}
   */
  code: null
});
