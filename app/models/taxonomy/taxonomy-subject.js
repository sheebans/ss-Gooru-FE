import Ember from 'ember';

/**
 * Taxonomy Subject
 *
 * @typedef {Object} TaxonomySubject
 */
export default Ember.Object.extend({

  /**
   * @property {string} id - Subject ID
   */
  id: null,

  /**
   * @property {string} title  - Subject title
   */
  title: null

});
